import { doNothing } from '../../core/utils';

/* Exports */
const type = {
	props: {
		actions: {},
	},
	normalize: doNothing,
	parse: doNothing,
	simple: false,
	setup: doNothing,
	type: 'source',
};

export default type;
