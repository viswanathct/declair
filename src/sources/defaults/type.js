import { doNothing } from '../../core/utils';

/* Exports */
const type = {
	props: {
		actions: {},
	},
	normalize: doNothing,
	parse: doNothing,
	setup: doNothing,
	type: 'source',
};

export default type;
